import 'package:flutter/material.dart';

class ProfileInformationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Profile Information')),
      body: Center(child: Text('Profile Information Details Here')),
    );
  }
}
