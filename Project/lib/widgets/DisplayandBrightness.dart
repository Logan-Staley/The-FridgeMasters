import 'package:flutter/material.dart';

class DisplayandBrightness extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Display and Brightness'),
      ),
      body: Center(
        child: Text('Content for Display and Brightness page'),
      ),
    );
  }
}
