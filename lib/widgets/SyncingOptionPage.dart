import 'package:flutter/material.dart';

class SyncingOptionsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Syncing Options')),
      body: Center(child: Text('Syncing Options Details Here')),
    );
  }
}
